import { StarRepositoryDirective } from './star-repository.directive';

describe('StarRepositoryDirective', () => {
  it('should create an instance', () => {
    const directive = new StarRepositoryDirective();
    expect(directive).toBeTruthy();
  });
});
